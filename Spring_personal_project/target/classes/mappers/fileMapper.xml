<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.project.repository.FileDAO">
	<insert id="insertFile">
		insert into
		file(uuid,save_dir,file_name,file_type,bno,file_size)
		values(#{uuid},#{savedir},#{filename},#{filetype},#{bno},#{filesize})
	</insert>
	<select id="getFileList"
		resultType="com.ezen.project.domain.FileVO">
		select * from file where bno = #{bno}
	</select>
	<delete id="removeFile" parameterType="String">
		delete from file where uuid = #{uuid}
	</delete>
</mapper>